# 🖥️ 音乐软件 - 做成桌面软件版（网页版永远不消失）

本项目已集成 **Tauri v2** 桌面壳。桌面软件打开时直接嵌入你云端永远在线的网址：
```
https://music-app-production-9498.up.railway.app
```
所以 **网页版永远不会消失**，两边共用同一个云端数据库，用户在软件版里注册、写的歌，网页版登录后都能看到，100% 同步。

---

## 🎁 最终效果

| 形态 | 访问方式 | 电脑关机？ | 说明 |
|---|---|---|---|
| **🌐 网页版（永不消失）** | https://music-app-production-9498.up.railway.app | 不影响 | 任何设备（手机/电脑/平板）浏览器打开即可使用 |
| **💻 桌面软件版（Windows .exe / .msi）** | 双击安装后，桌面快捷方式打开 | 不需要开自己的电脑（数据全在云端） | 有独立窗口、任务栏图标，体验像 QQ 音乐 / 网易云音乐 |
| **🍎 桌面软件版（macOS .dmg / .app）** | 双击安装到「应用程序」文件夹 | 同上 | 适配 Intel + Apple Silicon 通用包 |

---

## 🚀 Windows 打包成 .exe 安装包（3 步）

### 第 1 步：下载项目代码到你 Windows 电脑
把整个项目文件夹下载 / 拷贝到你自己的 Windows 电脑上（例如 `D:\music-app\`）。

### 第 2 步：安装 2 个运行环境（一次性，装完不再用管）
1. **Node.js LTS**：去 https://nodejs.org/zh-cn 下载安装 LTS 版（一路下一步）
2. **Rust**：去 https://www.rust-lang.org/tools/install 下载 `rustup-init.exe`，运行一路下一步

### 第 3 步：双击运行脚本
直接双击项目里的：
```
桌面软件打包-Windows.bat
```
等 3-10 分钟（首次会下载 Rust 依赖，慢一些）。打包完成后，安装包在：
```
src-tauri\target\release\bundle\
  ├─ msi\音乐创作软件_1.0.0_x64_zh-CN.msi   ← 推荐，双击安装
  └─ nsis\音乐创作软件_1.0.0_x64-setup.exe  ← 兼容旧系统
```
把 `.msi` 发给别人双击安装，即可使用。

---

## 🚀 macOS 打包成 .dmg 安装包（3 步）

### 第 1 步：下载项目代码到你 Mac 电脑
拷贝整个项目到 Mac（例如 `~/music-app/`）。

### 第 2 步：安装环境（一次性）
1. **Node.js LTS**：https://nodejs.org/zh-cn
2. **Rust**：终端执行一行命令：
   ```bash
   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
   ```

### 第 3 步：运行脚本
在项目目录右键「服务」→「新建位于文件夹位置的终端窗口」，执行：
```bash
chmod +x 桌面软件打包-mac.command
./桌面软件打包-mac.command
```
打包完成后，DMG 安装包在：
```
src-tauri/target/universal-apple-darwin/release/bundle/dmg/音乐创作软件_1.0.0_universal.dmg
```
双击 `.dmg` 拖进「应用程序」即可安装。

---

## 🔄 更新机制（推荐）

**推荐就用当前这套「纯壳 + 云端网页」方案**，原因：
- ✅ 你改功能 / 修 bug，只要在 Trae IDE 里说一句「帮我推到 Railway 更新」，我跑 `railway up` 推送
- ✅ 用户不管是桌面软件还是网页版，**下次打开 / 刷新一下就看到新版**，不用重新下载安装包
- ✅ 桌面软件版体积只有 ~8MB，不占空间，启动快
- ✅ 网页版永远存在，用户没装软件时直接通过网址访问

（如果你要做成「本地离线版」，不依赖任何云端，也能做，但那样数据就不同步了，每次更新用户要重新下载安装包。除非你明确要求，不推荐。）

---

## 🎯 代码结构（已配置完成）

- `package.json`：新增 `app:build` / `app:build:windows` / `app:build:mac` 打包脚本
- `src-tauri/tauri.conf.json`：Tauri 配置，窗口尺寸 1280×800，加载云端 URL
- `src-tauri/Cargo.toml`：Rust 依赖（Tauri v2 core + window + shell 插件）
- `src-tauri/src/lib.rs`：桌面壳主入口
- `桌面软件打包-Windows.bat`：Windows 一键打包脚本
- `桌面软件打包-mac.command`：macOS 一键打包脚本
- [Dockerfile](file:///workspace/Dockerfile)：网页版云端部署（已成功部署到 Railway，永远在线）
